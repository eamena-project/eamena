APP_NAME = "curator"
APP_VERSION = semantic_version.Version(major=0, minor=1, patch=0)
APP_ROOT = os.path.dirname(os.path.abspath(inspect.getfile(inspect.currentframe())))
MIN_ARCHES_VERSION = semantic_version.Version(major=7, minor=6, patch=4)
MAX_ARCHES_VERSION = semantic_version.Version(major=7, minor=6, patch=10)
